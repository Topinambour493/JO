export type ContactType = {
    id: string,
    first: string,
    last: string,
    avatar: string | undefined,
    notes: string |undefined,
    favorite: boolean,
}

export type favoriteType = {
    id: string,
    favorite: boolean,
}
